# ИИшница mini-app

Telegram Mini App для приватного клуба ИИшница. React + Vite + Tailwind + Telegram WebApp SDK.

Auto-deploy на GitHub Pages при пуше в main.

## Локально

```bash
bun install
bun run dev   # http://localhost:5180
bun run build # → dist/
```
